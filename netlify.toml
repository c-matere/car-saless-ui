[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10.2.4"
  NODE_ENV = "production"

# Handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
